import { SearchArmorsPipe } from './search-armors.pipe';

describe('SearchArmorsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArmorsPipe();
    expect(pipe).toBeTruthy();
  });
});
