import { SearchTalismansPipe } from './search-talismans.pipe';

describe('SearchTalismansPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTalismansPipe();
    expect(pipe).toBeTruthy();
  });
});
