import { SearchIncantationsPipe } from './search-incantations.pipe';

describe('SearchIncantationsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchIncantationsPipe();
    expect(pipe).toBeTruthy();
  });
});
